Make the shield logo animation device-aware. Today both mobile and desktop pull the 1.3 MB WebM. Switch phones to the existing 79 KB shield_animation_mobile.mp4 and tablet/desktop to the existing 90 KB shield_animation.mp4, falling back to WebM only if .mp4 playback fails. Keep the static-frame fade-in behavior identical so there's no visual regression. Single biggest perf win.

Retire the legacy team high-five JPEG. Replace the 803 KB .jpg reference in the testimonials background with the existing 220 KB .webp (use <picture> with the JPEG as fallback if any browser support concern, otherwise just the WebP). Delete the orphaned .jpg from public/images/ once nothing references it.

Resize oversized logo and fallback assets. Re-encode staticinsureitlogo.webp and insure_it_logo.webp from 4224×1444 down to ~800 px wide; re-encode shield_lastframe.webp from 1920×1080 down to ~600 px wide. These files display at far smaller sizes; the source dimensions are wasteful. Verify the navbar and shield-logo positioning still look correct on mobile and desktop after re-encoding.

Make the hero preload mobile-aware. The unconditional <link rel="preload" href="/images/heroimage1.webp"> in the root layout sends a 287 KB desktop image to phones that will never display it. Use two preload tags with media queries — the desktop hero gated to (min-width: 641px) and the mobile hero gated to (max-width: 640px).

Convert the navbar logo to next/image. The non-large Logo variant in client/src/components/logo.tsx renders a plain <img>. Swap to next/image with explicit width/height and a sizes attribute so phones download a small variant and CLS stays at zero. Verify the /_next/image optimizer responds correctly through the custom Express server (it should, since the server delegates all non-API routes to Next.js).

Tighten testimonials overlay for WCAG AA contrast. White testimonial heading and label text currently sit over the high-five photo with only browser default contrast — the photo's brightness varies and the text fails the 4.5:1 ratio in lighter regions. Add or darken a dark-tinted overlay (e.g. a bg-black/45 to bg-black/55 layer between image and text) so contrast holds across the whole image. Verify contrast in DevTools or a checker before considering done.

Validate. Run a Lighthouse mobile audit locally (or via PageSpeed Insights post-deploy) and confirm Performance ≥ 90 and LCP < 2.5 s. Spot-check homepage and About on a real phone viewport for visual regressions.